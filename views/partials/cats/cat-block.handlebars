<li>	 
	{{name}}
	{{#if sleep}}
	<button class="change-sleep" data-id="{{id}}" data-newsleep="{{sleep}}">
	DEVOUR IT
	</button>
	{{else}}<button class="delete-cat" data-id="{{this.id}}">DELETE</button>
    {{/if}}
</li>
 
