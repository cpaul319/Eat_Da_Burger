 
<li> 
	{{name}}
	{{#if devour}}
	<button class="change-devour" data-id="{{id}}" data-newDevour="{{devour}}">
	DEVOUR IT
	</button>
	{{else}}<button class="delete-burger" data-id="{{this.id}}">DELETE</button>
    {{/if}}
</li>

 

 
 
 
 
